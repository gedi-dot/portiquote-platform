# Deployment files run on Linux. A CRLF checkout from Windows makes the
# shebang read "bash\r" and every script fails with "not found".
Dockerfile          text eol=lf
.dockerignore       text eol=lf
deploy/**           text eol=lf
*.sh                text eol=lf
